{
  "name": "tetetetennis",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "logic:build": "moon build --target js --release",
    "logic:test": "moon test",
    "mocap:extract": "./.venv-mocap/bin/python scripts/mocap/extract_joint_track.py",
    "mocap:stroke": "node scripts/mocap/extract-stroke-keyframes.mjs",
    "mocap:trim": "./.venv-mocap/bin/python scripts/mocap/trim_joint_track.py",
    "mocap:serve": "node scripts/mocap/extract-serve-keyframes.mjs",
    "predev": "npm run logic:build",
    "dev": "vite",
    "prebuild": "npm run logic:build",
    "build": "vite build",
    "preview": "vite preview",
    "test": "moon test",
    "test:js": "moon test --target js"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^6.0.0"
  }
}
